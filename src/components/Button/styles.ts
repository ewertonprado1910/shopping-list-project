import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
    container: {
        width: "100%",
        height: 48,
        alignItems: "center",
        justifyContent: "center",
        backgroundColor: "#2c4091",
        borderRadius: 5,
    },
    title: {
        color: "#f1f1f1",
        fontSize: 14,
        fontWeight: 200
    }
})