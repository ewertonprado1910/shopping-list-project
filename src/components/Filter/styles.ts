import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        gap: 5,

    },
    title: {
        fontSize: 15,
        fontWeight: 600

    },
})